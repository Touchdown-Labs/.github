name: Touchdown Free CI self-test

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  free-ci:
    uses: Touchdown-Labs/.github/.github/workflows/free-ci.yml@v1
    with:
      profile: check-only
